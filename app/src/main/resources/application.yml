spring:
  application:
    name: letsConnect

  data:
    redis:
      host: "redis-14598.crce182.ap-south-1-1.ec2.redns.redis-cloud.com"
      password: ${REDIS_PASSWORD}
      username: "default"
      port: 14598
      connect-timeout: 5000ms
      timeout: 2000ms

  datasource:
#    url: jdbc:postgresql://db.itnqijodbvgvktcevpoh.supabase.co:5432/postgres
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres
    username: postgres.itnqijodbvgvktcevpoh
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 12000000
      validation-timeout: 5000
      pool-name: SpringBootHikariCP
      data-source-properties:
        prepareThreshold: 5
        preparedStatementCacheQueries: 256
        preparedStatementCacheSizeMiB: 5

        tcpKeepAlive: true
        socketTimeout: 30
        connectTimeout: 10
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
        ddl-auto: update

jwt:
  secret: ${JWT_SECRET_BASE64}
  expiration-minutes: 15

letsconnect:
  email:
    verification:
      expiry-hour: 24
    reset-password:
      expiry-minutes: 30
